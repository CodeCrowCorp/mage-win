<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MageWin.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MageWin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" >

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <AppBar Closing="AppBar_Closing">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="ChannelButton" Click="myButton_Click" Content="Enter channel id" Background="Transparent" Width="200" Height="70">
                </Button>
                <Grid x:Name="TextGrid" Width="600" Visibility="Collapsed" Background="CadetBlue">
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="ChannelText" Width="300" Margin="10" PlaceholderText="Enter Channel Id"></TextBox>
                        <Button Background="CadetBlue" Content="Submit" Width="250" Click="Button_Click" Height="50"></Button>
                    </StackPanel>

                </Grid>
            </StackPanel>
        </AppBar>
        <StackPanel Orientation="Vertical" Grid.Row="1" HorizontalAlignment="Center">
            <TextBox Header="Enter channel id:" PlaceholderText="Channel id" Width="300"></TextBox>
            <Button Background="CadetBlue" Content="Submit" Width="300" Height="70" Click="Button_Click"></Button>
        </StackPanel>
    </Grid>

</Window>
